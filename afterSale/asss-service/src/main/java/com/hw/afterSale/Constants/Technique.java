package com.hw.afterSale.Constants;

import com.thinvent.common.util.StringUtil;

import java.lang.reflect.Array;
import java.util.HashMap;
import java.util.Map;

/**
 * Created by huangwei on 2017/5/16.
 */
public class Technique {

    /*public String getAndSql(String table,Map<String, Object> params) throws Exception{
        String sql = "";
        Map<String, Object> condition = new HashMap<>();
        String[] array;
        for (int i=0; i < params.size(); i++){
            if (!StringUtil.isNullOrEmpty(params.get(params.keySet()).toString())) {
                sql += " and " + table + "." + params.keySet() + "= :" + params.keySet();
                condition.put(params.keySet().toString(), params.get(params.keySet().toString()));
            }

        }
        array = new String[]{sql, condition.toString()};

        return null;
    }*/








}
